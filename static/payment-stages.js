document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.stage-config').forEach(b=>{const e=b.querySelector('[name="has_stages"]'),c=b.querySelector('.stage-count'),f=b.querySelector('.stage-fields'),v=(b.dataset.values||'').split(',').map(x=>x.trim()).filter(Boolean);const r=()=>{const n=Math.max(1,Math.min(5,Number(c.value)||1));c.value=n;f.innerHTML='';for(let i=1;i<=n;i++){const l=document.createElement('label'),x=document.createElement('input');l.textContent=`第 ${i} 阶段金额`;x.name=`stage_amount_${i}`;x.type='number';x.step='0.01';x.min='0';x.required=e.checked;x.value=v[i-1]||'';l.appendChild(x);f.appendChild(l)}f.hidden=!e.checked;c.closest('label').hidden=!e.checked};e.addEventListener('change',r);c.addEventListener('input',r);r()})});
