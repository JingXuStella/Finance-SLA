services:
  - type: web
    name: finance-ledger
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: finance-ledger-files
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: finance-ledger-db
          property: connectionString
      - key: UPLOAD_FOLDER
        value: /var/data/uploads
databases:
  - name: finance-ledger-db
    databaseName: finance_ledger
    user: finance_user
